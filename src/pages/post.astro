---
//siempre se usa el async para el await, en el caso de astro al hacerlo dentro de estas lineas el tiene algo llamao top level await que significa que ya no necesito usar el async
const response = await fetch("https://jsonplaceholder.typicode.com/posts");

const data = await response.json();

console.log(data);
---

<style>
  div{
    background-color: rgb(0, 0, 0);
    color: darkolivegreen;
  }
</style>

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>pousts</title>
  </head>
  <body>
    <h1>posts</h1>
    {
      data.map((obj) => (        <div>
          <h2>{obj.title}</h2>
          <h3>User id : {obj.id}</h3>
          <p>{obj.body}</p>
        </div>))
    }
  </body>
</html>
